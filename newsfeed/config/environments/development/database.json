{
  "defaultConnection": "default",
  "connections": {
    "default": {
      "connector": "mongoose",
      "settings": {
         "uri": "${process.env.DATABASE_URI || 'mongodb://newsfeeddb-usr:e7kLrKm07JWykd25sQflPs@10.0.0.10:27116' }",
        "database": "${process.env.DATABASE_NAME || 'newsfeed'}"
      },
      "options": {
        "ssl": false
      }
    }
  }
}